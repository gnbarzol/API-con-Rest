/*Agregamos una capa en cache, el cache lee el tiempo en segundos */
const FIVE_MINUTES_IN_SECONDS = 3000;
const SIXTY_MINUTES_IN_SECONDS = 360;

module.exports = {
    FIVE_MINUTES_IN_SECONDS,
    SIXTY_MINUTES_IN_SECONDS
};